extends _layout
block form
	form(action="/apply/photo-upload" method="POST")
		input(type="hidden" value=submission._id name="id")
		.box-head.box-head-accent-oob
		if errors 
			include partials/errors
		include partials/photo-upload